{
  "name": "lerna-repo-test",
  "private": true,
  "scripts": {
    "delete:tag": "git tag -l | xargs -n 1 git push --delete origin & git tag | xargs git tag -d"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
